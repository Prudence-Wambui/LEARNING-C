#include<stdio.h>
int main()
{
    int minutesPerHour = 60; //Good variable name

    int m = 60; // Ok, but not so easy to understand what m actually is

    int myNum = 10; 
    int mynum =10;

    //The two variables above are not the same.
    //This is because C programming is case sensitive

    return 0;

}